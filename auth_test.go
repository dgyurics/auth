package main

import (
	"testing"
)

func TestLogin(t *testing.T) {
	// expected := "logging in"
	// got := login()
	// if got != expected {
	//   t.Errorf("unexpected return value: %q", got)
	// }
}
